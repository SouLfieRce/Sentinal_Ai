{
  "name": "sentinel-ai",
  "version": "1.0.0",
  "description": "SENTINEL AI — Cybersecurity Intelligence Platform",
  "scripts": {
    "dev": "concurrently -n server,client -c blue,cyan \"node server/index.js\" \"npm run client\"",
    "client": "cd client && npm run dev",
    "install:all": "cd client && npm install && cd ../server && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
